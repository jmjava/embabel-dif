package com.embabel.dif.domain;

public enum RelationType {
    REQUIRES,
    PRESERVES,
    CONFLICTS_WITH,
    IMPLEMENTS,
    VERIFIED_BY,
    DERIVED_FROM,
    DEPENDS_ON,
    AFFECTS,
    SUPERSEDES
}
